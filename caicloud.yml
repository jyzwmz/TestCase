integration:
  image: localhost:5000/admin/ubuntu:latest
  commands:
    - cd /
    - echo "integration"
prebuild:
  image: localhost:5000/admin/ubuntu:latest
  commands:
    - echo "prebuild"
postbuild:
  image: localhost:5000/admin/ubuntu:latest
  commands:
    - echo "postbuild"
